<h1> Create a New Shopping List, <%= @user.name %> </h1>

<form action="/shopping_lists" method="post">
    <label>New List Name</label<
    <input type="text" id="list_name" name="shopping_list[name]" value="Food List">
    <br>
    <h3>Items to add to List</h3>
    <% Item.all.each do |item| %>
        <label><%= item.name %> $<%= item.price %></label>
        <input type="checkbox" name="shopping_list[item_ids][]" id="item_<%= item.id %>" value="<%= item.id %>">
        <br>
    <% end %>

    <h3>Create New Item</h3>
    <label>Item Name</label>
    <input type="text" id="item_name" name="item[name]">
    <label>Item Price</label>
    <input type="number" id="item_price" name="item[price]">
    
    <input type="submit" value="Create List">
    <input type="reset">
</form>

<h3> Need to add another Item to the database, before making your list? </h3>
<a href="items/new"><button>Click Here!</button></a>